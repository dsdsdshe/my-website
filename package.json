{
  "name": "mindquantum-website",
  "version": "0.1.0",
  "private": true,
  "description": "Static website for MindQuantum with Eleventy and Sphinx-built docs.",
  "scripts": {
    "dev": "eleventy --serve",
    "build": "eleventy",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}

